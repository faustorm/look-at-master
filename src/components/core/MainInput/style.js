import colors from '../../../constants/colors';
import styles from '../../../constants/styles';

export default {
  input: {
    fontSize: styles.pageFontSize,
    borderWidth: 0.5,
    color: colors.regularLighterText,
    borderColor: colors.ultraTransparent,
    textAlign: 'center',
  },
  view: {
    paddingVertical: 20,
    paddingHorizontal: 0,
  },
};
